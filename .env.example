# SwingScout — refresh script secrets (LOCAL / CI ONLY, never shipped to the browser).
# In production this lives only in GitHub repo secrets (Settings → Secrets → Actions).
# Copy to .env for local live runs:  cp .env.example .env

POLYGON_API_KEY=your_polygon_free_tier_key_here
